---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: preview-pr-{PR_NUMBER}-apps
  namespace: flux-system
spec:
  interval: 5m
  dependsOn:
    - name: preview-pr-{PR_NUMBER}-infra
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./flux/clusters/preview-pr-{PR_NUMBER}/apps
  prune: true
  wait: true
  timeout: 10m
  decryption:
    provider: sops
    secretRef:
      name: sops-age
