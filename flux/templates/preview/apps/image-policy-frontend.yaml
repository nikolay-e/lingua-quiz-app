apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: frontend
  namespace: preview-pr-{PR_NUMBER}
spec:
  imageRepositoryRef:
    name: frontend
  policy:
    numerical:
      order: asc
  filterTags:
    pattern: '^pr-{PR_NUMBER}-[a-f0-9]+-(?P<ts>[0-9]+)$'
    extract: '$ts'
