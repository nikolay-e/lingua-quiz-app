apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: backend
  namespace: preview-pr-202
spec:
  imageRepositoryRef:
    name: backend
  policy:
    numerical:
      order: asc
  filterTags:
    pattern: '^pr-202-[a-f0-9]+-(?P<ts>[0-9]+)$'
    extract: '$ts'
