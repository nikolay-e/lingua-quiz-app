apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: lingua-quiz
  namespace: lingua-quiz-staging
spec:
  interval: 5m
  chart:
    spec:
      chart: ./helm/lingua-quiz-app
      sourceRef:
        kind: GitRepository
        name: lingua-quiz-repo
        namespace: flux-system
      interval: 1m
  values:
    registry: ghcr.io/nikolay-e/lingua-quiz
    imagePullSecrets:
      - name: ghcr-secret
    backend:
      image:
        tag: main # {"$imagepolicy": "lingua-quiz-staging:backend"}
    frontend:
      image:
        tag: main # {"$imagepolicy": "lingua-quiz-staging:frontend"}
  valuesFrom:
    - kind: ConfigMap
      name: lingua-quiz-values
      valuesKey: values.yaml
    - kind: Secret
      name: lingua-quiz-sops-values
      valuesKey: values.yaml
