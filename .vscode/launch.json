{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Node.js (live-server)",
      "type": "node",
      "request": "attach",
      "port": 9229,
      "restart": true,
      "protocol": "inspector",
      "skipFiles": ["<node_internals>/**"],
      "outFiles": ["${workspaceFolder}/src/**/*.js"]
    }
  ]
}
