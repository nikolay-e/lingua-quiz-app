# word-processing

Многоязычная система генерации и валидации CEFR-словарей для LinguaQuiz.

## Архитектура: 3 основных модуля

### 1. Генерация частотных списков → 2. Анализ слов → 3. Валидация миграций

**Поток данных:**

```text
Субтитры (50k слов)
    ↓
[1. Генерация частотных списков]
    ↓ (top-N наиболее частотных)
[2. Анализ слов] (7-стадийный pipeline)
    ↓ (обработанные слова)
[3. Валидация миграций] (проверка качества)
    ↓
Migration JSON для бэкенда
```

### Принципы проектирования

**✅ DRY (Don't Repeat Yourself):**

- Централизованные тестовые данные (`test_data.py`)
- Переиспользуемые fixtures (`conftest.py`)
- Общий код для всех языков

**✅ Многоязычность через конфигурацию:**

- **ВСЁ в config.yaml** (единый источник истины), **НЕ в коде**
- **Blacklist** слов: секция `languages.{lang}.blacklist` (profanity, abbreviations, interjections, etc.)
- **Whitelist** для тестов: секция `languages.{lang}.filtering.test_whitelist`
- Правила нормализации: секция `languages.{lang}.normalization`
- Параметры фильтрации: секция `languages.{lang}.filtering`

**Поддерживаемые языки:** EN, ES, DE, RU (расширяется через конфигурацию)

**Почему всё в config.yaml (best practice)?**

- ✅ Единый файл - проще редактировать и версионировать
- ✅ YAML с комментариями - понятнее, чем множество TXT файлов
- ✅ Структурированные данные - категории blacklist (profanity, abbreviations, etc.)
- ✅ Никаких внешних TXT файлов - всё централизовано

## Протестированная функциональность (только интеграционные тесты)

### 1. Генерация частотных списков (WordSource: 26 тестов) ✅

**SubtitleFrequencySource** - извлечение слов из субтитров:

- Парсинг CSV формата `word<TAB>frequency`
- Извлечение top-N наиболее частотных слов
- Смещение начального rank (пропуск первых N)
- Обработка пустых файлов

**FrequencySource** - библиотека wordfreq:

- Получение слов из корпусов
- Метаданные с rank для приоритизации

**MigrationFileSource** - существующие миграции:

- Парсинг JSON с метаданными
- Анализ покрытия

**CustomListSource** - пользовательские файлы:

- TXT, JSON, CSV форматы

**Общие возможности:**

- Метод `count()` без итерации
- Корректный `language_code`

### 2. Анализ слов в миграциях (VocabularyProcessor: 16 тестов) ✅

**7-стадийный pipeline обработки:**

1. **Нормализация** - правила из `normalization_rules.yaml`
2. **Лемматизация** - Stanza (95.5% точность): `comiendo → comer`
3. **NLP-анализ** - POS-теги, NER, морфология
4. **Валидация** - blacklist из `data/blacklists/{lang}.txt`
5. **Фильтрация Named Entities**
6. **Фильтрация склонений** - коэффициенты из `config.yaml`:
   - ES: 0.4 (60% удаляется)
   - DE: 0.2 (80% удаляется)
   - RU: 0.15 (85% удаляется)
7. **Дедупликация** - приоритет базовой формы

**Возможности:**

- Режимы: с фильтром / без / только леммы
- Ограничение `target_count`
- Сбор статистики фильтрации
- Категоризация по частям речи
- Граничные случаи (пустой источник, всё отфильтровано)

**Мультиязычность:** EN, ES, DE, RU

### 3. Валидация миграций (MigrationValidator: 20 тестов) ✅

**Поиск дубликатов:**

- Внутри файла (нормализованное сравнение)
- Между файлами (cross-file validation)
- Без учёта регистра/пробелов

**Проверка качества:**

- Пустые поля
- Placeholder'ы (`word="word"`)
- Одинаковые source/target
- Слишком длинные слова (>100 символов)

**Валидация ID:**

- Дублирующиеся пары (source_id, target_id)
- Большие разрывы (порог в `config.yaml`)

**Валидация синтаксиса ответов:**

- `[]` - опциональный: `world [universe]`
- `,` - все обязательны: `floor, apartment`
- `|` - альтернативы: `car|automobile`
- `()` - группировка: `(equal|same), (now|immediately)`
- Обнаружение несбалансированных скобок

**Обработка ошибок:**

- Некорректный JSON
- Ошибки доступа
- Сериализация результатов

## Конфигурация языковых особенностей

**ВСЁ определяется в config.yaml (единый источник истины), НЕ в коде:**

### config.yaml - реальная структура

```yaml
languages:
  es:
    name: 'Spanish'
    wordfreq_code: 'es'
    spacy_models: ['es_core_news_sm']

    # Правила нормализации
    normalization:
      unicode_normalization_form: 'NFC'
      preserve_diacritics: true # ñ, é, á сохраняются
      articles: ['el', 'la', 'los', 'las']
      remove_hyphens: false
      comma_separator: false

    # Blacklist (категории запрещённых слов)
    blacklist:
      profanity: ['puta', 'mierda', 'coño', 'carajo']
      abbreviations: ['dr', 'sr', 'etc']
      interjections: ['oh', 'ah', 'uh', 'hey']
      anglicisms: ['of', 'and', 'the', 'internet']
      slang: ['pa', 'po', 'che', 'tío']
      proper_nouns: ['méxico', 'españa', 'madrid', 'josé']

    # Фильтрация и whitelist для тестов
    filtering:
      min_word_length: 2
      max_word_length: 20
      inflection_frequency_ratio: 0.4 # 60% склонений удаляется
      short_word_whitelist: ['no', 'sí', 'ya', 'me', 'mi']
      test_whitelist: ['libro', 'mesa', 'silla', 'gato', 'perro', 'casa', 'estar', 'comer', 'tener'] # Для тестов ТОЛЬКО
      ner_frequency_threshold: 0.0005
      exclude_patterns:
        - "^\\d+$"
        - '^[A-Z]{2,}$'

    # Категории POS-тегов
    pos_categories:
      essential_nouns: ['NOUN', 'PROPN']
      essential_verbs: ['VERB', 'AUX']
      essential_adjectives: ['ADJ']
      function_words: ['DET', 'PRON', 'ADP', 'CONJ', 'SCONJ']
```

**Преимущества централизации в config.yaml:**

- ✅ Единый файл - легко редактировать и версионировать
- ✅ YAML с комментариями - понятнее структура
- ✅ Категоризация blacklist (не просто список слов)
- ✅ test_whitelist встроен в filtering (логическая группировка)

## Запуск тестов

```bash
# Все тесты (62 теста, ~10 секунд)
pytest tests/integration/ -v

# Параллельно (14 workers)
pytest tests/integration/ -n auto

# Конкретный модуль
pytest tests/integration/test_word_source.py -v
pytest tests/integration/test_vocabulary_processor.py -v
pytest tests/integration/test_migration_validator.py -v
```

## Покрытие тестами

| Модуль                 | Тесты  | Покрытие | Статус |
| ---------------------- | ------ | -------- | ------ |
| WordSource (генерация) | 26     | ~80%     | ✅     |
| VocabularyProcessor    | 16     | ~60%     | ✅     |
| MigrationValidator     | 20     | ~75%     | ✅     |
| **Итого**              | **62** | **~72%** | ✅     |

Время: **~10 секунд** (параллельно, 14 workers)

## Использование CLI

Новый CLI на базе **Typer + Rich** с красивым выводом и автокомплитом.

### Интерактивный режим (рекомендуется)

```bash
# Запуск без аргументов → интерактивное меню
python -m vocab_tools.cli.app

# Или через алиас (если установлен пакет)
vocab-tools
```

**Возможности интерактивного режима:**

- ✅ Автокомплит для команд (Tab)
- ✅ Автокомплит для языков/уровней с fuzzy matching
- ✅ Real-time валидация ввода
- ✅ История команд (↑/↓)
- ✅ Цветная таблица доступных команд
- ✅ Ctrl+C для отмены текущей операции
- ✅ `exit` для выхода

### Прямое выполнение команд

**Analyze** - Анализ словарей:

```bash
vocab-tools analyze es-a1
vocab-tools analyze spanish-a1 --format json
vocab-tools analyze de-b1 --top-n 2000 --output ./reports
```

**Validate** - Валидация миграций:

```bash
vocab-tools validate
vocab-tools validate --only structure
vocab-tools validate --only quality
vocab-tools validate --format json
```

**Generate** - Генерация частотных списков:

```bash
vocab-tools generate            # Все языки
vocab-tools generate es         # Только Spanish
vocab-tools generate de --top-n 5000
```

**Fill** - Автозаполнение placeholder'ов:

```bash
vocab-tools fill es-a1
vocab-tools fill spanish-a1
```

**History** - История выполнения:

```bash
vocab-tools history
vocab-tools history --limit 20
vocab-tools history --format json
```

### Автокомплит для shell

```bash
# Установка автокомплита (bash/zsh/fish)
vocab-tools --install-completion

# Показать скрипт автокомплита
vocab-tools --show-completion
```

### Rich форматирование

CLI использует Rich для красивого вывода:

- **Цветные таблицы** для результатов валидации
- **Progress bars** с анимацией
- **Древовидные структуры** для статистики
- **Панели** для важных сообщений
- **Syntax highlighting** для JSON

**Пример вывода analyze:**

```text
╭─ ANALYZING ES A1 VOCABULARY ─────────────────────────╮
│ Generating comprehensive analysis report              │
╰───────────────────────────────────────────────────────╯

Language:        ES
Level:           A1
Top-N words:     1,000
Output:          /path/to/reports

📊 Vocabulary Statistics
├─ Total words: 1,245
├─ Unique lemmas: 987
├─ 📚 In Vocabulary
│   ├─ High frequency (<1k): 876
│   ├─ Moderate (1-5k): 98
│   └─ Very rare (>10k): 13
└─ 🔍 Missing Words (total: 124)
    ├─ 🔴 Critical (< 100): 45
    ├─ 🟠 High priority (< 500): 67
    └─ 🟡 Medium priority (< 1000): 12

✅ ANALYSIS COMPLETE
```

## Структура проекта

```text
vocab_tools/
├── config/
│   ├── config_loader.py           # Загрузка конфигурации
│   └── models.py                  # Pydantic модели (будет добавлено)
├── core/
│   ├── vocabulary_processor.py    # [2] 7-стадийный pipeline
│   ├── stanza_lemmatizer.py       # Лемматизация
│   └── word_source.py             # [1] Источники (частотные списки)
├── validation/
│   └── migration_validator.py     # [3] Валидация миграций
└── analysis/
    └── a1_analyzer.py             # Анализ покрытия

config.yaml                        # ⚙️ ЕДИНЫЙ источник конфигурации
                                   # Содержит: blacklist, test_whitelist,
                                   # normalization, filtering для всех языков

data/
└── subtitle_frequencies/          # 📊 Частотные списки из субтитров
    ├── es_50k.txt
    ├── de_50k.txt
    └── en_50k.txt

tests/integration/
├── conftest.py                    # Session fixtures (DRY)
├── test_data.py                   # Централизованные данные (DRY)
├── test_word_source.py            # [1] 26 тестов ✅
├── test_vocabulary_processor.py   # [2] 16 тестов ✅
└── test_migration_validator.py    # [3] 20 тестов ✅
```

## TDD Workflow

1. Определить ожидаемое поведение в этом файле
2. Написать интеграционный тест (провалится)
3. Реализовать (пройдёт)
4. Отрефакторить
5. Проверить отсутствие регрессий

## Требования к производительности

- Генерация словаря из 1000 слов: < 30 секунд
- Валидация migration файла: < 2 секунды
- Все тесты: < 30 секунд
