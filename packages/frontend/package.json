{
  "name": "@linguaquiz/frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "predev": "npm run build --workspace=@linguaquiz/core",
    "dev": "vite",
    "build": "npm run build --workspace=@linguaquiz/core && vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-check --tsconfig ./tsconfig.json --watch"
  },
  "dependencies": {
    "@linguaquiz/core": "file:../core"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.0.0",
    "@tsconfig/svelte": "^5.0.4",
    "@types/node": "^24.0.14",
    "svelte": "^5.36.0",
    "svelte-check": "^4.2.2",
    "svelte-preprocess": "^6.0.3",
    "typescript": "^5.8.3",
    "vite": "^7.0.4"
  }
}
