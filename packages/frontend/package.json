{
  "name": "@linguaquiz/frontend",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "start": "live-server --port=8080 src",
    "test": "npm run test:unit && npm run test:integration && npm run test:component",
    "test:unit": "NODE_ENV=test jest --coverage --detectOpenHandles unit",
    "test:integration": "NODE_ENV=test jest --coverage --detectOpenHandles integration",
    "test:component": "NODE_ENV=test jest --detectOpenHandles component",
    "build": "mkdir -p dist && cp -r src/* dist/ && echo \"Build completed: $(date)\" > dist/build-info.txt"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "axios": "^1.7.3",
    "dompurify": "^3.1.7",
    "jest-localstorage-mock": "^2.4.26",
    "jwt-decode": "^4.0.0",
    "live-server": "^1.2.0"
  },
  "description": "",
  "devDependencies": {
    "jest-environment-jsdom": "^29.7.0",
    "jest-fetch-mock": "^3.0.3",
    "node-fetch": "^3.3.2"
  }
}
