{
  "name": "@linguaquiz/frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node --inspect ./node_modules/.bin/live-server --port=8080 src",
    "build": "mkdir -p dist && cp -r src/* dist/ && cp -r src/index.html src/login.html dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.3",
    "https": "^1.0.0",
    "live-server": "^1.2.2",
    "vite": "^5.4.1",
    "vite-plugin-static-copy": "^1.0.6"
  },
  "description": "",
  "devDependencies": {
    "vite-node": "^2.0.5"
  }
}
